package main

templ about(workExperience []ExperienceEntry, schoolExperience []ExperienceEntry) {
	@header("Adrien's blog")
	<body>
		@navbar()
		<div class="body-main">
			@resume(workExperience, schoolExperience)
		</div>
		@footer()
	</body>
}
